import React from "react";

const Header = () => {
  return (
    <header className="h-header bg-white px-8 py-6 border-b border-gray-300">
      <div className="flex items-center justify-between">
        <nav className="flex items-center"> This is a Header </nav>
      </div>
    </header>
  );
};

export default Header;
