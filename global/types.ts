export type ImageType = {
  name: string
  size: number
  type: string
  url: string
}

export type PaginationType = {
  total?: number
  perPage?: string
  currentPage?: string
  totalPages?: number
}